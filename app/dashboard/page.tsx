import { Dashboard } from "@/components/dashboard";

export const metadata = {
  title: "Where you left off — AI Investment Committee",
  robots: { index: false, follow: false }
};

export default function DashboardPage() {
  return <Dashboard />;
}
