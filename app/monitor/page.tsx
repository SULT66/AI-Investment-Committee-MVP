import { Monitor } from "@/components/monitor";

export const metadata = {
  title: "Monitor — AI Investment Committee",
  robots: { index: false, follow: false }
};

export default function MonitorPage() {
  return <Monitor />;
}
