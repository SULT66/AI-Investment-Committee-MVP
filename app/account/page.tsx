import { AccountPanel } from "@/components/account-panel";
import "./account.css";

export const dynamic = "force-dynamic";
export const metadata = { title: "Account — AI Investment Committee" };

export default function AccountPage() {
  return <AccountPanel />;
}
