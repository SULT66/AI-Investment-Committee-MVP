import { AdminPanel } from "@/components/admin-panel";

export const metadata = {
  title: "Staff — AI Investment Committee",
  robots: { index: false, follow: false }
};

export default function AdminPage() {
  return <AdminPanel />;
}
